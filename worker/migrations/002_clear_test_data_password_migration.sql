-- Migration: パスワードハッシュ方式の変更に伴うデータクリア
--
-- パスワードハッシュを SHA-256(固定ソルト) から PBKDF2(ランダムソルト) に変更しました。
-- 既存ユーザーのパスワードハッシュは新方式と互換性がないため、ログインできなくなります。
--
-- このSQLは「テストデータのみで本番データがない」場合に、関連テーブルを
-- すべてクリアして作り直すためのものです。本番データがある場合は実行しないでください
-- （その場合は、全ユーザーにパスワード再設定を案内する等の移行手順が別途必要です）。
--
-- D1のConsoleタブで実行してください。外部キー制約 (ON DELETE CASCADE) により、
-- usersを削除すると関連する vehicles, vehicle_members, fuel_records,
-- maintenance_records, api_tokens, sessions も連動して削除されます。

DELETE FROM users;
